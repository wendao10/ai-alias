# Plugin local data — contains the user's real-name → alias mapping.
# NEVER commit this; it must stay private on the user's machine.
data.json

# Build artifacts (only if a bundler is added later)
node_modules/
