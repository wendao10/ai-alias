# Obsidian plugin data (contains the real-name → alias mapping; must never be published)
data.json

# Build / dependency artifacts
node_modules/
main.js.map

# Optional eslint / editor caches
.DS_Store
*.log
